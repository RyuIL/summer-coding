import React from "react";
import { Icon, Button } from "semantic-ui-react";

const AddButton = () => {
  return (
    <Button icon>
      <Icon name="plus" />
    </Button>
  );
};

export default AddButton;
